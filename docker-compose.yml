services:
  model-foundry:
    build: .
    container_name: model-foundry
    restart: unless-stopped
    ports:
      - "7352:7352"
    volumes:
      - model_foundry_config:/app/config
    environment:
      - NODE_ENV=production
      - HOME=/app/config
volumes:
  model_foundry_config:
